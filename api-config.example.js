/**
 * Copy this file to `api-config.js` and set your deployed backend URL.
 * The Vercel site only serves static HTML; Flask (`server.py`) must run elsewhere
 * (Railway, Render, Fly.io, a VPS, etc.).
 *
 * Example:
 *   window.THE_LEDGER_API_BASE = 'https://your-app.up.railway.app';
 */
window.THE_LEDGER_API_BASE = '';
